# A11545 (A011545) — AUDIT REPORT (oeis4 round, 2026-08-26)

**Target:** `FormalConjectures/OEIS/11545.lean` @ pin `2411d22e`.
**Class:** HOLD_NUMERIC ×2 (both claims infinitary; per-term numeric evidence).

## Primary source (gate a)

OEIS A011545 (%N): "a(n) is the integer whose decimal digits are the first
n+1 decimal digits of Pi." %F: "a(n) = floor(Pi*10^n)" (matches Lean def).
%C: collision-count interpretation + "Wolfgang Haken (1977) conjectured that
no term of this sequence is a perfect square, and estimated the probability
that this conjecture is false to be smaller than 10^-9." b-file head
3,31,314,3141,... reproduced exactly (12 terms vs mpmath floors).

## Declarations under audit

conjecture1 `∀ n, ¬ IsSquare (a n)`; conjecture2 no integer strictly inside
(π·10ⁿ, π/arctan(10⁻ⁿ)) ∀n — both universally quantified over ℕ ⇒
HOLD_NUMERIC labels only.

## Independent computation (gate c)

Path 1: mpmath dps=6260 π; floor → a(n); exact integer-sqrt square test:
**no square for n ≤ 6000** (min distance-to-nearest-square = 1 occurs only at
n=0 since a(0)=3 sits between 1 and 4).
Path 2 (interval reformulation, independent arithmetic): integer lies strictly
inside iff frac(π·10ⁿ) + width ≥ 1 with width = π/arctan(10⁻ⁿ) − π·10ⁿ ≈
π·10ⁿ·10^{-2n}/3: **no containment n = 1..1200**; closest approach at
n = 761 with slack 1.63·10^-7 (a genuine near-roll of π's digit stream —
recorded as evidence texture). Precision guards: dps exceeds needed digits by
60; margins far above mp error everywhere scanned.

## Verdict

Definitions faithful to %F/%C (incl. strict inequalities as formalized);
evidence positive in range; both remain legitimately open ⇒ HOLD_NUMERIC,
no claims. No prior art upstream (0 hits for A011545).

Gates: a ✓ · b ✓ · c ✓ · d ✓
Scripts: scripts/a11545_pi.py · Log: a11545_run1.log
