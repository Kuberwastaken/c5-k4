# ErdosProblems/288 — two-interval reciprocal sums

**File:** `FormalConjectures/ErdosProblems/288.lean` @ `2411d22e`.
**Statements:** finitely many pairs of intervals `I₁,I₂ ⊂ ℕ⁺` with `∑_{n∈I₁} 1/n + ∑_{n∈I₂} 1/n ∈ ℕ`;
variant `|I₂| = 1`; variant general k. Source page (erdosproblems.com/288, fetched 2026-08-26) marks the
problem "open … cannot be resolved with a finite computation" and gives the example
1/3+1/4+1/5+1/6+1/20 = 1 (i.e. I₁=[3,6], I₂={20}).

**Faithfulness:** Lean encodes intervals as `ℕ⁺ × ℕ⁺` pairs with `Set.Icc`, sums in `ℚ`, integer result as
`∃ n : ℕ+, ... = n`. Matches prose; no missing coprimality/ordering hypotheses in source to drop.
Note the source example is recovered verbatim by our search below — extra faithfulness confirmation.

**Computation (exact `Fraction` prefix sums):** all solutions to the `|I₂|=1` variant with every entry ≤ 400:
```
([1,1],c=1,sum 2), ([1,2],c=2,sum 2), ([1,3],c=6,sum 2),
([2,2],c=2,sum 1), ([2,3],c=6,sum 1), ([3,6],c=20,sum 1)
```
— six sporadic solutions, the last being precisely the source's advertised example.
Sanity sub-check: single-interval integer reciprocal sums occur only for [1,1] up to 400 (Kürschák's
classical highest-power-of-2 obstruction, confirmed computationally).

**Result class:** `HOLD_BOUNDED` data record (finiteness itself is not search-decidable; the enumeration
shows scarcity and that all small solutions are known-sporadic). Status coordinates unchanged:
open/open/faithful/literal-finiteness. Duplicate surface clean (no issues/PRs on 288 found).
