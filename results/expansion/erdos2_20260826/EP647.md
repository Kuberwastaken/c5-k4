# ErdosProblems/647 — divisor-sum max problem (RETRO after duplicate gate)

**File:** `FormalConjectures/ErdosProblems/647.lean` @ `2411d22e` (byte-identical to current `main`).
**Statement:** `answer(sorry) ↔ ∃ n > 24, ⨆ m : Fin n, m + σ 0 m ≤ n + 2` — is there n > 24 with
max_{m<n}(m+τ(m)) ≤ n+2? Known true at n = 24.

## Parse disambiguation (defect checked, REJECTED)

Two readings of the unparenthesized binder: (A) `(⨆ m, m + τ(m)) ≤ n+2` (faithful max-form);
(B) `⨆ m, (m + τ(m) ≤ n+2)` ≡ `∃ m < n, ...` (trivially true at n=25 → would be a #4992-class defect).
Reading (A) is the actual elaboration: the same file's
`twenty_four : ⨆ m : Fin 24, ↑m + σ 0 ↑m ≤ 26 := by exact ciSup_le <| by decide`
typechecks only under (A) (`ciSup_le` proves `iSup f ≤ c` with numeric `f`; under (B) the goal is a bare
`Prop`-lattice `iSup` and `exact ciSup_le` cannot unify). Corroborated upstream: Mathlib states `iSup_le`
as `⨆ i, f i ≤ a`. The independent issue #5021 confirms "the elaborated terms agree".

## Finite search performed

Three exact implementations: sympy rolling-max scan to 2·10⁵; sieve-based scan to 5·10⁶; direct constraint
check `∀ k ∈ [1,n): τ(n−k) ≤ k+2` recovering exactly {24} in [20,40) (DB-sanity ✓ vs upstream `twenty_four`).
**No n > 24 exists up to 5·10⁶** in any run; all runs completed inside the 60 s cap.

## Duplicate gate outcome (decisive)

`gh search issues` returns **open issue #5021** (Millennium Research, 2026-08-17): a kernel-checked
bounded exclusion `∀ n, 24 < n → n ≤ 10^8 → ¬(⨆ m : Fin n, ↑m + σ 0 m ≤ n + 2)` as a witness-chain
certificate (891,554 witnesses), `#print axioms` clean; the issue additionally cites prior computational
searches by Idén (10¹²) and Hughes/bentrd (9.17×10¹⁸).

**Result class:** `STOP_DUPLICATE_PRIOR_ART` / retro-confirmation. Our bracket (25..5·10⁶) is strictly
inside the already-published kernel-checked bracket and the far larger literature brackets. No novelty;
recorded for ledger completeness and because our three-path recomputation independently confirms the
exclusion at the smaller bracket with exact arithmetic.

Status coordinates: (1) open; (2) no full formal solution anywhere (bounded exclusions exist);
(3) faithful; (4) literal ∃-statement above.
