# ErdosProblems/931 — solution census audit

**File:** `FormalConjectures/ErdosProblems/931.lean` @ `2411d22e`.
**Statement:** `erdos_931 : answer(sorry) ↔ ∀ᵉ (k₁ : ℕ) (k₂ ≥ 3), k₂ ≤ k₁ → {(n₁, n₂) | n₁ + k₁ ≤ n₂ ∧ (∏ i ∈ Icc 1 k₁, n₁+i).primeFactors = (∏ j ∈ Icc 1 k₂, n₂+j).primeFactors}.Finite`

Are there only finitely many pairs of disjoint blocks of consecutive integers (lengths `k₁ ≥ k₂ ≥ 3`) whose products have identical prime support?

**Faithfulness:** matches erdosproblems.com/931 prose ("finitely many n₂ ≥ n₁+k₁ ... same prime factors"); ordering hypothesis present; `primeFactors` is the support set (no multiplicity) matching prose. The `additional_condition` variant (`n₂ ≤ 2(n₁+k₁)`) likewise faithful — its wording was fixed by merged upstream PR #11 ("allowing for a finite number of counterexamples", following the website update); the main finiteness statement was untouched by that PR. No defect found.

**Finite-search census (band 2).** For fixed `(k₁,k₂)` the predicate is decidable per pair; enumerated all solutions over windows of numbers ≤ B (exact sieve-based smallest-prime-factor supports):

| (k₁,k₂) | solutions ≤ B=4·10⁵ | ≤ B=1.5·10⁶ | last solution found |
|---|---|---|---|
| (3,3) | 16 | **16** | (2649, 58562) |
| (4,4) | 5 | 5 | (151, 339) |

Counts plateau hard; e.g. for (3,3): 15 at B=2·10⁴ → 16 at 10⁵ → 16 at 4·10⁵ → 16 at 1.5·10⁶.
Smallest solutions recovered: (3,3): (2,7) — blocks {3,4,5} vs {8,9,10}, both supported on {2,3,5}; (4,4): (3,7).
DB-sanity gate: searcher independently reproduces the upstream `additional_condition_nonempty` witness
`(k₁,k₂,n₁,n₂) = (10,3,0,13)` exactly (its LHS 1..10 and RHS 14,15,16 both have support {2,3,5,7});
an initial off-by-one in our window indexing was caught precisely by this gate and corrected.

**Independent recomputation:** second implementation via sympy `factorint` (no shared code path) reproduces
all 15 (3,3)-solutions and all 5 (4,4)-solutions in the common range ≤ 3000, identically.

**Result class:** `HOLD_BOUNDED` — complete censuses: (3,3) has exactly 16 solutions with max(n₂)=58562 inside
numbers ≤ 1.5·10⁶; (4,4) has exactly 5 with max(n₂)=339. No infinite family visible; the open finiteness
statement survives. A timeout never occurred; brackets are exact.

**Novelty/duplicate:** gh search — only hits are closed PRs #2589/#3113 (formalizing the known
`additional_condition_nonempty` witness) and merged #11 (variant rephrasing). No prior solution-census
artifact for the main statement found. Status coordinates: (1) open; (2) no formal solution of the main
statement anywhere; (3) faithful; (4) literal assertion = finiteness for every (k₁,k₂), untouched by our data.
