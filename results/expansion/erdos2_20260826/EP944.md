# ErdosProblems/944 — vertex-critical graphs without small critical edge sets

**File:** `FormalConjectures/ErdosProblems/944.lean` @ `2411d22e`.
**Statement:** `∀ k ≥ 4, ∀ r ≥ 1, ∃ V G, G.IsErdos944 k r` where IsErdos944 = vertex-critical of chromatic
number k with every critical edge set of size > r. Dirac's conjecture is the r=1 subcase.

**Status-sync audit (angle 4):** Skottová–Steiner, arXiv:2508.08703 ("Critical edge sets in
vertex-critical graphs", Aug 2025) construct (k,r)-graphs for every **k ≥ 5** and r ≥ 1 (order Θ(k³r³)),
extending Jensen 2002 (r=1) and Lattanzio 2002. This resolves the declaration for all k ≥ 5; the open core
is exactly **k = 4** for arbitrary r — still open, so the declaration's status remains correctly "open" at
the pin and on erdosproblems.com. No status mismatch ⇒ no sync action; recorded as partial-progress note.
Degeneracy check: Mathlib's criticality predicates cannot fire on ∅ or non-edges (removing no edges keeps χ),
so no vacuous-falsity defect. Existence claim ⇒ not refutable by finite search; constructing a k=4 witness
would solve Dirac's conjecture (out of scope).

**Result class:** `STATUS_NOTE` (all four coordinates: open/open/faithful/∃-statement; literature delta
recorded). Duplicate surface clean.
