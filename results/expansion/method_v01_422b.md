# Method v0.1 development trial: WOWII 422b

**Target:** Written on the Wall II 422b
**Trial class:** development set; not held out
**Started:** 2026-08-12 UTC
**Primary outcome:** `HOLD_BOUNDED`

## Phase 0 — frozen target and provenance

The current WoW II page marks 422b `O` and prints, dated December 8, 2010:

> Let G be a connected graph on n > 3 vertices and M the vertices of maximum
> degree. Then i(G) <= alpha(G[M]) + gamma(G[V-M])^2.

Primary page:
`http://cms.uhd.edu/faculty/delavinae/research/wowII/open.html`.
The target occurs in the repository snapshots
`data/wowii-conjectures.json` and `corpora/graffiti_pc_wow2.json`.

The page's own definitions freeze the single reading:

- `i(G)` is the minimum cardinality of an independent dominating set;
- `M` is the set of all maximum-degree vertices;
- `alpha(H)` is the independence number of the indicated induced subgraph;
- `gamma(H)` is the domination number of the indicated induced subgraph;
- `gamma(empty)=0`, the standard totalization needed for regular graphs.

The stray extra right parenthesis printed after `G[M]` is typographical and
does not create a second mathematical reading. Outcome class: `UNAMBIGUOUS`.

The contemporaneous paper E. DeLaViña, R. Pepper, and B. Waller,
*Graffiti.pc on the Independent-Domination Number of a Graph* (Congressus
Numerantium, 2011; dated 2010 in the PDF) was recovered from
`https://www.uhd.edu/documents/academics/sciences/dpw-cn2011.pdf`. Its glossary
confirms the same meanings of `i`, `alpha`, induced-subgraph notation, and
maximum-degree vertices. It discusses 422a and 422c but does not state, prove,
or refute 422b.

At upstream `google-deepmind/formal-conjectures` main commit
`547f309edcc2069c1f61c2465729031c10385540`, no Lean file, issue, PR, or code
search hit covers WoW II 422b. The local corpus metadata likewise records
`in_formal_conjectures: false`. This trial therefore studies an existing open
WoW II target that could become a new upstream declaration; it is not already
an upstream Lean declaration.

## Phase 1 — database-sanity gate

The earlier campaign gate recorded 997 applicable controls and no violation.
Method v0.1 requires the larger fixed control list, so this trial reruns from
scratch on:

- every connected Graph Atlas graph of order 4 through 7;
- `C5` through `C9`, `P7`, Petersen, `K3,3`, and `K7`;
- stars `K1,r` for `2 <= r <= 7`;
- complete bipartite `K(a,b)` for `2 <= a <= 4` and `a <= b <= 5`.

All four quantities are computed exactly by subset enumeration. The fresh gate
checked 992 applicable connected Atlas graphs and 24 named controls, with zero
violations. Twenty-three Atlas controls attained equality. Gate output is
written incrementally to `method_v01_422b_search.jsonl` by
`scripts/search_wowii_422b_blowups.py`.

## Residual and theorem baseline

Use the signed residual

```text
R(G) = alpha(G[M]) + gamma(G[V-M])^2 - i(G).
```

The conjecture asserts `R(G) >= 0`.

For every regular graph, `M=V`, so

```text
R(G) = alpha(G) - i(G) >= 0.
```

This is the elementary theorem baseline that every independent dominating set
is independent and hence has cardinality at most `alpha(G)`. Equality occurs
when every maximal independent set has maximum size. It explains the repeated
regular-family wall and proves that no regular carrier or uniform blow-up of a
regular quotient can cross 422b.

## Quotient reduction and obstruction

Let `Q[K_s]` be a positive clique blow-up with blob sizes `s_v`. Then exactly:

```text
i(Q[K_s])                         = i(Q)
alpha(Q[K_s][M])                  = alpha(Q[M])
gamma(Q[K_s][V-M])                = gamma(Q[V-M])
deg(blob v) + 1                   = s_v + sum_{u~v} s_u.
```

Thus the weights affect the residual only by selecting the maximum weighted
closed-degree blob set `M`. Uniform weights on a regular quotient force `M=V`
and remain behind the theorem baseline.

### Pre-search directional prediction

To decrease `R`, nonuniform weights must make a proper set `M` uniquely
maximum while:

- `alpha(Q[M])` stays small, preferably because `Q[M]` is a clique;
- `gamma(Q[V-M])` stays one, so the complement of `M` has a universal vertex;
- `i(Q)` remains at least three.

At quotient order at most nine, the exact search also measures the maximum
observed `i(Q)`. Whenever `i(Q) <= 4`, a negative residual necessarily has
`gamma(Q[V-M])=1`, since nonempty `M` gives `alpha(Q[M])>=1` and
`gamma>=2` would already make the right side at least five.

The max-set realizability constraints are

```text
z_v = s_v + sum_{u~v} s_u
z_m = t                 for m in M
z_x <= t - 1            for x outside M
s_v >= 1, sum_v s_v <= 100.
```

A numerical integer optimizer is permitted only to propose weights. Every
positive witness is rechecked with exact integer arithmetic. Solver
infeasibility is not used as an exact negative certificate.

## Declared bounded construction search

- all connected unlabelled quotients on 4 through 9 vertices, generated by
  nauty `geng -cq`;
- every exact residual-eligible quotient partition;
- positive integer blob weights with expanded order at most 100 if a negative
  quotient residual survives;
- eight workers and a hard 60-second wall-clock cap;
- incremental JSONL after every completed order/shard;
- no labelled random search and no post-result expansion of the bounds.

### Honest pre-run note

Before this durable report was written, an implementation probe checked the
Graph Atlas quotients through order seven, then all 11,117 connected quotients
of order eight. It found zero negative quotient partitions; a ten-second
partial order-nine probe checked 15,548 quotients and also found zero. These
are development/debugging observations, not prospectively logged evidence.
The complete capped run below is the auditable result.

## Search result

The complete run finished in 26.07 wall-clock seconds, below the declared
60-second cap:

```text
connected quotients exhausted, orders 4..9: 273,189
order-9 quotients:                         261,080
quotients with i(Q) > 2:                    51,791
maximum i(Q) observed:                           5
negative quotient residual partitions:           0
integer max-set realization calls:                0
witnesses:                                         0
```

All 64 order-nine `geng` shards completed and their counts sum to 261,080.
Together with the complete lower orders, this equals the known connected
unlabelled graph count through order nine. The final JSONL summary records
`complete: true`, 69 of 69 tasks, and zero witnesses.

This is stronger than failure to find weights of expanded order at most 100.
For every connected quotient `Q` on at most nine vertices and every nonempty
proper subset `M`, the exact quotient residual is nonnegative. Therefore no
positive clique blow-up of any expanded order can violate 422b when its
quotient has at most nine vertices. The weight optimizer was never invoked.

The optimized negative-partition enumerator was independently compared with a
brute-force all-partitions implementation on all 992 connected Atlas graphs;
the outputs agreed exactly. Separately, 40 random positive clique blow-ups of
Atlas quotients were explicitly expanded, and direct expanded-graph
recomputation agreed with all three quotient identities (`i`, `alpha` on `M`,
and `gamma` off `M`). The JSONL was also checked for all 64 distinct order-nine
shards, the expected totals, and a complete summary.

No candidate exists inside the declared family and bounds. There is no graph6
or adjacency witness to save and no novelty claim to make.

## Method changelog entry

**Prediction:** nonuniform clique weights might select a proper, low-alpha
maximum-degree set while leaving its complement dominated by one vertex, thus
crossing the regular `alpha-i` wall.

**Outcome:** `HOLD_BOUNDED`; the proposed separation never reached even a
negative quotient residual on any connected quotient through order nine.

**Failed assumption:** degree-set asymmetry was not the only obstruction. In
this bounded universe the base invariants themselves obey

```text
i(Q) <= alpha(Q[M]) + gamma(Q[V-M])^2
```

for every set `M`, whether or not weights can realize it as a maximum-degree
set. Max-set feasibility and the order-100 integer bound were therefore
irrelevant.

**Transformation-table update:** positive clique blow-up is strongly negative
for this target: it preserves all residual terms at quotient level and cannot
manufacture a crossing from a quotient/set pair that already satisfies the
inequality. Future work on 422b should first pursue a theorem explaining the
all-partitions bounded hold or use a transformation that changes independent
domination relative to the quotient. Merely enlarging blow-up weights is
mathematically pruned.

This inference was learned only after the complete search and belongs to the
development method; it cannot be silently imported into a future held-out
protocol.
