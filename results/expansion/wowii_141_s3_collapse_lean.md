# Lean extraction: the WOWII #141 `S3`-to-`Z3` collapse

Date: 2026-08-13 UTC

Local certificate: `lean/Wow141S3Collapse.lean`

## Result

The structural obstruction learned from the prospective permutation-cover
trial is formalized without importing or replaying the 31-variable solver
trace.

For `S3 = Equiv.Perm (Fin 3)`, the module defines fixed-point-freeness and the
two oriented 3-cycles, then proves:

1. `fixedPointFree_classification`: a permutation of three letters is
   fixed-point-free exactly when it is one of the two 3-cycles;
2. `alternatingGroup_classification`: `A3` consists exactly of the identity
   and those two cycles;
3. `fixedPointFree_mem_alternatingGroup`: every fixed-point-free generator is
   in `A3`;
4. `list_prod_mem_alternatingGroup` and `list_prod_classification`: every
   ordered product of fixed-point-free generators stays among the same three
   elements;
5. `closure_le_alternatingGroup`: the subgroup generated by any set of such
   generators is contained in `A3`;
6. `fixedPointFree_products_commute`: independently ordered products commute,
   so no nonabelian degree of freedom remains;
7. `product_fixedPointFree_iff_ne_one`: under the generator hypotheses, a
   cycle product is fixed-point-free exactly when it is not the identity.

The last theorem is the exact reusable boundary for the cover constraints.
Once singleton fundamental-cycle conditions force every edge permutation to
be fixed-point-free, every longer monodromy computation has reduced to the
three-element cyclic subgroup `A3 isomorphic to Z3`.

## Trust and build audit

Final warning-as-error elaboration:

```text
timeout 60s lake env lean -DwarningAsError=true \
  /Users/kuber.mehta/Projects/c5-k4/lean/Wow141S3Collapse.lean
```

Result: exit code zero in 6.9 seconds, with no warnings or errors.

The source contains no `sorry`, `admit`, `axiom`, or `native_decide`.  The
finite `S3` classifications use kernel-reduced `decide`, while the generic
list/set consequences use ordinary subgroup reasoning.

`#print axioms` reports only the standard mathlib logical foundations on the
four audited public results:

```text
[propext, Classical.choice, Quot.sound]
```

There are no custom axioms.

## Scope

This certificate does not assert the 423-constraint instance is infeasible;
that exact finite computation remains in the prospective trial ledger.  It
formalizes the general mathematical reason that the attempted `S3` escape
collapses to the cyclic subgroup whenever every generator is individually
forced to be fixed-point-free.

No commit, push, issue, PR, release, or other public action was made by this
lane.
