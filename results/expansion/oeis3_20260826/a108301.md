# A108301 — audit + REPAIR CANDIDATE (digital sums of Fermat numbers)

**Class: HOLD + COMPLETE FORMALIZATION-REPAIR CANDIDATE (textbook-sorry).
Gates: a ✓, b ✓, c ✓ (two paths), d ✓.**

## Source vs declaration

oeis.org/A108301 (%I #10 Dec 15 2017): a(n) = digit sum of F_n = 2^(2^n)+1.
Lean @ `2411d22e`: definition faithful; tests a_0..a_4 = 3,5,8,14,26 ✓.

Textbook-sorry in file:
`primes_in_a : (a 0).Prime ∧ (a 1).Prime ∧ (a 5).Prime ∧ (a 6).Prime ∧
(a 7).Prime ∧ (a 11).Prime`.

## Verification (gate c; two independent digit-sum paths)

(`scripts/a108301_digitsums.py`; logs `a108301_run1.log`, `a108301_run2.log`;
path 1 = int→str digit sum, path 2 = chunked divmod extraction)

| n | a(n) | prime? |
|---|---|---|
| 0 | 3 | yes |
| 1 | 5 | yes |
| 5 | 59 | yes |
| 6 | 89 | yes |
| 7 | 167 | yes |
| 11 | 2741 | yes |

Context composites: a(2)=8, a(3)=14, a(4)=26, a(8)=377=13·29, a(9)=734.

**All six conjuncts verify → the sorry is closable.**

Hunt beyond n = 11: no further prime digit sums for n ≤ 25
(5624, 11120, 22166, ..., 45445442 — all composite).

## Repair sketch

Values are tiny: after unfolding digits (`Nat.digits` equation lemmas) each
conjunct reduces to numeral primality — `(3).Prime`, `(5).Prime`,
`(59).Prime`, `(89).Prime`, `(167).Prime`, `(2741).Prime` all close by
`decide` (largest is 2741, trivial for `Nat.decidablePrime`):

```lean
theorem primes_in_a : ... := by
  have h0 : a 0 = 3 := by norm_num [a]; rfl   -- or decide after simp [Nat.digits]
  ...
  exact ⟨by decide, by decide, by decide, by decide, by decide, by decide⟩
```

## Status

Complete repair candidate (small); also a natural companion to A107247's fix.
Research-open conjecture (∃ n > 11 prime) remains open; bracket extended to n=25.
