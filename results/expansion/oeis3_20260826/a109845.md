# A109845 — audit (lcm±1 sequence)

**Class: HOLD_BOUNDED. Gates: a ✓, b ✓, c ✓ (two paths), d ✓.**

The two OEIS descriptions (lcm-of-previous ± 1 vs polynomial recurrence
a(n) = a(n-1)^2 + (-1)^n(a(n-1)+1)) verified identical for n = 1..16 — and are
PROVABLY equal: consecutive-term coprimality collapses lcm(previous ∪ {t}) to
L*t. Primes: a(1..5) = 2,3,5,31,929 all prime; a(6)..a(15) composite (digit
cap 4000). Infinitude-of-primes conjecture untouched (audit-only shape).
`scripts/a109845_primes.py`, `a109845_run4.log`.
