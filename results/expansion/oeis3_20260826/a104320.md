# A104320 — audit (zeros in ternary 2^n; Sloane's a(n)>0 for n>15)

**Class: HOLD_BOUNDED (disproof hunt negative). Gates: a ✓, b ✓, c ✓, d ✓.**

Scanned n ≤ 4000: zero ternary digits of 2^n positive for every n > 15 —
no violation of the Lean formalization (∀ n > 15, a n > 0, research open).
b-file head (0,0,0,...) consistent. `scripts/a104320_ternary.py`,
`a104320_run1.log`. A counterexample would have been a disproof of Sloane's
conjecture as encoded; none found in range.
