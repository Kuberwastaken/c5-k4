# A108129 — audit (Riesel problem; 509203 smallest Riesel number?)

**Class: HOLD.** Gates: a ✓, b ✓, d ✓.

OEIS (`oeis_pages/A108129.html`): "%N Riesel problem: let k=2n-1; then a(n)=
smallest m >= 1 such that k*2^m-1 is prime, or -1 if no such prime exists"
and, verbatim: "It is conjectured that the integer k = 509203 is the smallest
Riesel number, that is, the first n such that a(n) = -1 is 254602."

The Lean declaration `a 254602 = -1 ∧ (∀ n : ℕ, 1 ≤ n ∧ n < 254602 → a n ≠ -1)`
is exactly this (509203 = 2·254602 − 1). The definition uses PNat to model
m ≥ 1 and returns −1 (as ℤ) when no exponent works — faithful including the
sentinel. Test values a(1)=2, a(2)=1, a(3)=2, a(4)=4→1 match %S head.

Status: Browkin–Schinzel proved 509203·2^k−1 composite for all k>0; minimality
(the ∀ direction) remains open and is actively worked (PrimeGrid). No defect.
