# A167604 — audit (Chua's Euclid–Mullin variant; every prime?)

**Class: HOLD.** Gates: a ✓, b ✓, d ✓.

OEIS (`oeis_pages/A167604.html`): a(n+1) = least prime dividing
∏_{i∈I}a(i) + ∏_{i∈I'}a(i), minimized over subsets I. Because the terms are
distinct primes (Euclid argument, noted on the page), subset products range
over exactly the divisors d of P=∏a(i) and the expression is d + P/d; hence
"minimize least-prime-factor over subsets" equals Lean's
`Nat.minFac (∏ d ∈ n.divisors, (d + n/d))` — an exact reformulation, and the
Lean file documents this equivalence. Test terms 2, 3, 5, 11 match %S head.
The open question "One can ask whether all primes occur" is encoded faithfully
as `∀ p, p.Prime → ∃ n ≥ 1, a n = p`. No defect found; no finite witness can
settle it in either direction within budget (Booker's arXiv:1605.08929 variant
cited on both sides is a different construction).
