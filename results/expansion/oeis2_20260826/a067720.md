# A067720 — audit (φ(k²+1) = k·φ(k+1); k=8 the lone exception?)

**Class: HOLD_BOUNDED.** Gates: a ✓, b ✓, c ✓ (two independent code paths),
d ✓.

## Source vs declaration

OEIS (`oeis_pages/A067720.html`): %N "Numbers k such that phi(k^2+1) =
k*phi(k+1)" = Lean `A k`. Comments: "a(n)+1 is prime except for a(5)=8" and
"Is a(5)=8 the only additional value? - Ralf Stephan, Feb 11 2004".
Lean research-open theorem `prime_add_one_of_a : A k → k ≠ 8 → Prime (k+1)`
captures this faithfully. The textbook direction
(k+1 and k²+1 both prime → member) checks out analytically (φ(pq) with
k²+1=k·(k+1)... precisely φ(k²+1)=k² and k·φ(k+1)=k² when both are prime).

## Verification

- Published b-file (10^4 members, max k = 1,548,870): every listed member ≠ 8
  has k+1 prime (sympy PRP check on all 9999 values).
- Primary enumeration (`a67720_primary.py`, sympy factorint + SPF-sieve φ):
  all members in [1, 60000] enumerated; published head reproduced exactly;
  the only member with composite k+1 is k=8.
- Independent enumeration (`a67720_independent.py`, own Pollard rho +
  deterministic Miller-Rabin + per-value trial-division φ): agrees on
  [2, 20000]; only suspect (8,9).

## Conclusion

No crossing found. The declaration remains open with strong computational
support; our independent bracket extends certified coverage to [1,60000]
beyond reliance on OEIS completeness.
