# A109227 — audit (prime-index primality strings)

**Class: HOLD.** Gates: a ✓, b ✓, d ✓.

## Source vs declaration

OEIS (`oeis_pages/A109227.html`): %e pins the encoding — "a(4) = 110101
because the string '00110101' has 1's at the prime indices 2,3,5,7 and the
first two zeros are dropped" — matching the Lean definition exactly (indicator
string of 0..p_n, leading zeros dropped, read as decimal digits; the
`ofDigits … .reverse` orientation is correct). The comment "a(2) and a(121)
are primes. Are there any more?" is faithfully encoded as
`∃ n > 0, n ≠ 2 ∧ n ≠ 121 ∧ Prime(a n)`.

## Analysis

a(n) has ~p_n decimal digits, so each primality test at index n costs a PRP on
an ~n log n-digit number; a heuristic Σ 1/(n log n) divergence suggests more
primes exist but with rapidly growing gaps — beyond the 60 s budget to find.
The declaration is faithful and open; no defect found.

Hand-verified head: a(1)=1, a(2)=11, a(3)=1101, a(4)=110101 against the
published %S terms.
