# A063880 — audit (σ(n) = 2·usigma(n); mod-216 and primitive-structure claims)

**Class: HOLD_BOUNDED.** Gates: a ✓ (page recovered), b ✓, c ✓ (sieve-based
exact enumeration), d ✓.

## Source vs declaration

Lean definitions (`unitaryDivisors`, `usigma`, `A`) match the OEIS notion;
three research-open declarations mirror the OEIS conjectures (all members
≡ 108 mod 216; primitive terms powerful with 108 unique), plus two textbook
sorried lemmas and one `research solved` sorried lemma
(`exists_primitive_of_a`).

## Verification (exact sieve, n ≤ 200000)

562 members found; first: 108, 540, 756, 1188, … (matches OEIS head).

- every member satisfies n ≡ 108 (mod 216) ✓ (no crossing);
- every member admits a decomposition d·s with d a member, s squarefree,
  gcd(d,s)=1 ✓ (supports `exists_primitive_of_a`);
- the only primitive member ≤ B is 108 ✓.

Bracket: any counterexample exceeds 200000.

## Recorded observation (not claimed as discovery)

`exists_primitive_of_a` carries `@[category research solved]` while its proof
is `sorry`. Upstream convention reserves that category for declarations with
actual proofs; this is a candidate low-glamour repair (supply proof or
downgrade the category). Not filed upstream by us.
