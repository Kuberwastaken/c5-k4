# A111114 — audit (floor(prime(n)/π(n)); infinitely many decreases?)

**Class: HOLD.** Gates: a ✓, b ✓, d ✓.

OEIS A111114: a(n) = integer part of prime(n)/π(n). Lean `a n =
Nat.nth Nat.Prime (n-1) / Nat.primeCounting n` matches (test values
3, 2, 3, 3 for n=2..5 check out by hand). The conjecture "infinitely many n
with a(n) > a(n+1)" is encoded as `∃ᶠ n in atTop, a n > a (n+1)` — the
standard frequent-occurrence rendering; faithful.

Falsifiability shape: a finite witness cannot refute an ∃ᶠ statement, so this
lane is audit-only. No defect found.
