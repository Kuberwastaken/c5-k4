# A081091 — audit (infinitely many primes with three 1-bits)

**Class: HOLD.** Gates: a ✓, b ✓, d ✓.

## Source vs declaration

OEIS (`oeis_pages/A081091.html`): "%N Primes of the form 2^i + 2^j + 1,
i > j > 0" and "%C Primes with exactly 3 bits set in their binary
representation. Sam Wagstaff (see paper) conjectured that there are infinite
primes with exactly 3 bits set."

Lean: `A p := p.Prime ∧ p.bits.count true = 3`, conjecture
`answer(sorry) ↔ Set.Infinite {p | A p}`. Equivalence of "exactly 3 set bits"
with "2^i+2^j+1, i>j>0" holds because a prime > 2 must have bit 0 set, so the
lowest of the three bits is 2^0. Faithful; Wagstaff (Exp. Math. 10 (2001))
reference present in both.

Open problem in genuine good standing (b-file to 7800 terms); nothing to sync,
no computable finite witness exists for an infinitude claim by search.
