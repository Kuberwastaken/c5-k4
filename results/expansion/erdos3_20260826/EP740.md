# EP 740 — audit (@ `2411d22e`, `FormalConjectures/ErdosProblems/740.lean`)

**Status: AUDIT_CLEAN (infinitary chromatic class).**

## Site text (erdosproblems.com/740, fetched 2026-08-26)
m infinite cardinal, χ(G) = m, r ≥ 1: must G contain a subgraph of chromatic number m with
no odd cycle of length ≤ r? Rödl: true for m = ℵ₀, r = 3. E-H strengthening via f_r(m);
girth variant also asked [Er81]. OPEN.

## Lean reading
`NoShortOddCycle G r := ∀ v, ∀ cycle walk c at v, Odd length → length > r`;
main: ∀ G, ℵ₀ ≤ chromaticCardinal → ∀ r, ∃ Subgraph H, H.coe.chromaticCardinal = G.chromaticCardinal ∧ NoShortOddCycle H.coe r.
- Matches the site statement (χ(H) = m preserved; "subgraph" non-induced ✓; odd-cycle
  avoidance via closed-walk cycles — standard simple-graph reading ✓).
- Walk-based cycle definition counts a graph containing ANY odd closed walk ≥ ... note:
  a closed odd walk of length > shortest implies an actual short odd cycle exists iff one
  has length ≤ r; NoShortOddCycle as stated requires ALL odd cycle walks to exceed r,
  which is equivalent to no odd cycle of length ≤ r in the underlying simple graph
  (any odd closed walk decomposes into cycles, at least one odd, each ≤ walk length).
  Sound encoding ✓.
- File's TODO (f_r(m) strengthening + girth variant from the site comment) = documentation
  scope gap, recorded; not a faithfulness defect.

Not bounded-evaluable (infinite cardinals). No defect found.
