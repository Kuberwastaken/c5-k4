# Invalid run 31852437717

- Classification: `INVALID_PRE_EVALUATION_OPEN_PR_PROTECTED_PATH_GATE`
- Workflow run: `31852437717` (attempt 1)
- Workflow: `Bondy longest cycles DEVELOPMENT v3.2 (disabled by default)`
- Event: `workflow_dispatch`
- Exact checked-out head: `9a097475f25601fc286d52694986ca33a114a0f8`
- Branch: `catchup-parity-packed-freeze`
- Job: `gated-development` (`failure`)
- Target enabled: `false`
- Target rows evaluated: `0`
- Target artifacts emitted: `0`
- Target results emitted: `0`

## Completed checks

Checkout, dependency installation, immutable v3.2 freeze verification, all 53
target-free tests, the exact `S(4,4)` source-control replay, and independent C++
replay compilation succeeded. The live gate then completed both source/status
brackets and emitted a canonical `GATE_FAIL` attestation. The target,
independent target verifier, enabled manifest, and enabled-artifact steps were
all skipped.

The attestation reports every check true except:

```text
no_open_pr_touches_protected_paths = false
```

In particular, it records all of the following as true:

- both complete bracket snapshots are byte-stable;
- live `main` is stable at commit
  `2411d22e1bd550d050d0eac6c1fb379a76a3e7c5`, tree
  `f6b52f1d3f63b365d6f8c405623d5f7a4e674efc`;
- the pinned target, declaration shape, 171-file semantic closure, toolchain,
  and external revisions are exact;
- the three-file pinned-to-live delta is disjoint from every protected path;
- open-PR bindings and search-result sets are complete and stable; and
- exact local contamination history passes with four freeze introducers.

## Exact strict stop

The complete open-PR surface contains 45 pull requests that touch at least one
semantic-closure or toolchain path. None touches the exact target file
`FormalConjectures/Arxiv/2606.03696/BondyLongestCycles.lean`. The matches are
mostly repository-wide dependency, utility, formatting, or maintenance work;
the frozen v3.2 policy nevertheless treats any protected-path touch as a
strict stop. The run therefore raised

```text
RuntimeError: source/status/duplicate gate failed closed
```

after writing the attestation and before target execution. This is a policy
stop, not source drift and not a mathematical result. Whether the rule is
needlessly stronger than the already frozen stable-main/stable-binding checks
must be decided in a separate target-free policy audit. It cannot be waived by
reinterpreting this failed run.

## Artifact record

GitHub uploaded one workflow artifact:

```text
id             9237940656
name           bondy-longest-cycles-development-v32-gate-evidence
size           161920 bytes
archive digest sha256:810f095f78e900e3971c9cb40cc9ca94a3a91399694da9188fcdf839565f174e
```

The extracted `source-status-attestation.json` is 846610 bytes with SHA-256
`321eaf92a5dbf90842f6fd3000f7f593bca52f65876d59c728b891ecb9e3a99d`.
The run was created at `2026-08-15T00:03:45Z` and completed at
`2026-08-15T00:05:13Z`.

This run establishes no candidate, counterexample, terminal search result, or
bounded result. It does not authorize activation, a release, an issue, a pull
request, or any other public mathematical claim.
