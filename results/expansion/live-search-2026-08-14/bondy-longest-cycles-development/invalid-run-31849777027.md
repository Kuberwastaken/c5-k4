# Invalid run 31849777027

- Classification: `INVALID_PRE_EVALUATION_GATE_INTEGRATION`
- Workflow run: `31849777027` (attempt 1)
- Workflow: `Bondy longest cycles DEVELOPMENT v3 (disabled by default)`
- Event: `workflow_dispatch`
- Exact checked-out head: `02442d86d571111631eb24b946a85ed1b295b8a8`
- Branch: `catchup-parity-packed-freeze`
- Job: `gated-development` (`failure`)
- Target enabled: `false`
- Target rows evaluated: `0`
- Target artifacts emitted: `0`
- Target results emitted: `0`
- Run artifacts: none (`total_count = 0`), so there are no artifact digests

## Step record

The checkout, Python setup, frozen dependency installation, immutable-freeze
verification, 47 target-free constructor/adversarial tests, exact `S(4,4)`
source-control replay, and independent C++ replay compilation all succeeded.
The `One complete v3 live continuity gate` step failed. Every target-processing,
independent-verification, enabled-manifest, and enabled-artifact step was
skipped. The always-run gate-evidence upload step completed with a warning
because `/tmp/source-status-attestation.json` did not exist, and uploaded
nothing.

## Exact pre-evaluation failure

The gate requested `GET /repos/google-deepmind/formal-conjectures/commits/main`
and then tested for `response["tree"]`. GitHub's commits API places this value
at `response["commit"]["tree"]["sha"]`, not at the response top level. The gate
therefore raised `RuntimeError: live main identity unavailable` before creating
a source/status attestation, constructing a live continuity object, or touching
the target process. This is a gate-integration failure, not evidence of source
drift and not a mathematical result.

## Dispatch-input record

The dispatch supplied `enable_target=false`, so both remaining strings were
inert and were never consumed by a target step. The supplied campaign commit
was `02442d8dcff07808abbead7bb876defc46ad760c`, which does not equal the actual
run head `02442d86d571111631eb24b946a85ed1b295b8a8`. The activation input was the
inert marker `DISABLED_TARGET_FREE`, not an activation preimage. These input
errors had zero target effect because target execution was disabled.

Separately, the activation preimage corresponding to the v3 sealed hash was not
preserved. That operational defect cannot be repaired by interpreting this run
or inventing a replacement preimage; any later activation requires an explicit
v3.1 reseal with a newly controlled guard. It does not change this run's
pre-evaluation classification.

This run is invalid as scientific or mathematical evidence. It establishes no
candidate, counterexample, terminal search result, source-status conclusion, or
public claim. It does not authorize a retry, activation, release, issue, pull
request, publication, or other public action.
