# Invalid runs 31854544552 and 31854645706

- Classification: `INVALID_PRE_EVALUATION_LIVE_GATE_DEADLINE`
- Workflow: `Bondy longest cycles DEVELOPMENT v3.3 (disabled by default)`
- Event: `workflow_dispatch`
- Exact checked-out head: `0d576acf096e3170bd5c1aeb6ad3bba0cf8d51db`
- Branch: `catchup-parity-packed-freeze`
- Target enabled: `false`
- Target rows evaluated: `0`
- Target artifacts emitted: `0`
- Target results emitted: `0`

## Completed checks

Both disabled runs passed checkout, frozen dependency installation, immutable
v3.3 freeze verification, all 61 target-free tests, the exact `S(4,4)` source
control, and warning-as-error compilation of the independent C++ replay. Each
then failed closed inside the complete live continuity gate. The target and
all post-target steps were skipped.

## Exact strict stop

The two attempts failed at the same operation in the second live bracket:

```text
after, after_rate_limits = bracket_snapshot(token, continuity)
graphql_open_pull_bindings(...)
final_rows, final_total = list_rows(OPEN_PULL_IDENTITY_QUERY)
TimeoutError: The read operation timed out
```

Run `31854544552` spent 58 seconds in the live-gate step before the final
GraphQL read timed out. A single controlled retry at the same immutable commit,
run `31854645706`, reproduced the stop after 59 seconds. The outer workflow
kept the live-gate process under its 60-second cap in both attempts.

Because the timeout occurred before the gate could finish and serialize a
canonical attestation, neither run produced an attestation artifact. GitHub's
artifact API reports zero artifacts for both run IDs. This is a repeated
operational deadline stop, not source drift, a failed mathematical candidate,
or evidence that any row satisfies or violates the target.

The v3.3 runs cannot be retried indefinitely or reinterpreted as passing. Any
continuation must use a separately reviewed freeze that preserves complete
open-pull enumeration, exact current/rename-away target-path collision checks,
head-OID and status stability, and all source/main/closure checks while fitting
the existing cap.

## Run record

| Run | Created | Completed | Job | Artifacts |
|---|---|---|---|---:|
| `31854544552` | `2026-08-15T00:45:00Z` | `2026-08-15T00:46:30Z` | failure | 0 |
| `31854645706` | `2026-08-15T00:46:56Z` | `2026-08-15T00:48:29Z` | failure | 0 |

These runs establish no candidate, counterexample, terminal search result, or
bounded result. They do not authorize activation, a release, an issue, a pull
request, or any other public mathematical claim.
