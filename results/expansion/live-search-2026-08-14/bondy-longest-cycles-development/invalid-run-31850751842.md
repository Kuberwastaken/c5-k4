# Invalid run 31850751842

- Classification: `INVALID_PRE_EVALUATION_SEMANTIC_CLOSURE_GATE`
- Workflow run: `31850751842` (attempt 1)
- Workflow: `Bondy longest cycles DEVELOPMENT v3.1 (disabled by default)`
- Event: `workflow_dispatch`
- Exact checked-out head: `adadbcf0924b7168b6f2548f3d54d73449bee195`
- Branch: `catchup-parity-packed-freeze`
- Job: `gated-development` (`failure`)
- Target enabled: `false`
- Target rows evaluated: `0`
- Target artifacts emitted: `0`
- Target results emitted: `0`
- Run artifacts: none (`total_count = 0`), so there are no artifact digests

## Step record

Checkout, Python setup, the frozen dependency installation, immutable v3.1
freeze verification, the target-free constructor/adversarial suite, exact
`S(4,4)` source-control replay, and independent C++ replay compilation all
succeeded. The `One complete v3.1 live continuity gate` step failed. The
target process, independent target-result verification, enabled evidence
manifest, and enabled evidence upload were all skipped. The always-run gate
evidence step uploaded nothing because the gate did not produce
`/tmp/source-status-attestation.json`.

## Exact pre-evaluation failure

The corrected REST identity parser accepted the real GitHub commits API shape
and reached semantic-import continuity. While recomputing the import closure of
the pinned upstream commit, `resolve_import_closure` encountered an external
Lean module whose prefix was not in the frozen allowlist `Mathlib, Lean` and
raised:

```text
RuntimeError: unfrozen external Lean import prefix
```

The exception occurred in `prepare_continuity` before either pinned/live
closure equality, source/status attestation emission, or target execution. The
frozen resolver deliberately fails closed on an unclassified external import;
this run therefore identifies another live-integration defect rather than
proving semantic drift. The emitted exception did not include the module name,
so that name is not inferred in this run record. Any correction must first
reproduce and identify the exact prefix, extend or otherwise correct the
closure model, reseal every affected hash, and pass a new independent
target-free audit.

## Dispatch and evidence status

The dispatch bound `campaign_commit` to the exact checked-out head. It supplied
`enable_target=false` and an inert non-secret marker for the workflow's
required activation input. Neither target guard was consumed because the
target step was skipped.

GitHub reports `created_at=2026-08-14T23:33:01Z`, job start
`2026-08-14T23:33:07Z`, job completion `2026-08-14T23:33:35Z`, and run update
`2026-08-14T23:33:36Z`. The run's artifacts endpoint reports
`total_count = 0`.

This run is invalid as scientific or mathematical evidence. It establishes no
candidate, counterexample, terminal search result, source-status conclusion,
or bounded result. It does not authorize activation, retrying the same frozen
bytes, a release, an issue, a pull request, or any other public claim.
