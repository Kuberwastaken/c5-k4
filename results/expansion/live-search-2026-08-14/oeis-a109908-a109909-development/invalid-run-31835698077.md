# Invalid run 31835698077

**Classification:** `INVALID_PRE_EVALUATION_GATE_TIMEOUT`

**Run:** <https://github.com/Kuberwastaken/c5-k4/actions/runs/31835698077>

## Identity and frozen input

- Workflow: `OEIS A109908 A109909 finite-prefix DEVELOPMENT`
- Event: `workflow_dispatch`
- Run / attempt: `31835698077 / 1`
- Created / completed: `2026-08-14T19:58:24Z / 2026-08-14T20:00:17Z`
- Head SHA: `d9ae09fe54af131790e72d12ad8b438a4b1fd9f6`
- `campaign_commit` input: `d9ae09fe54af131790e72d12ad8b438a4b1fd9f6`
- Frozen campaign: the joint A109908/A109909 finite-prefix residue-cover DEVELOPMENT trial, with 16 shards and candidate interval `1,000,000,001..1,500,000,000`.

The validation log exposed the input as `CAMPAIGN_COMMIT`, checked out that exact detached commit, and confirmed `git rev-parse HEAD` was the same SHA. The frozen harness and its unit tests passed before any target evaluation.

## Job audit

| Job | Conclusion | Started | Completed | Meaning |
|---|---|---|---|---|
| `validate-frozen-harness` | success | `19:58:29Z` | `19:58:46Z` | Exact immutable freeze and target-free tests passed. |
| `prepare-source-status-gate` | failure | `19:58:50Z` | `20:00:17Z` | Fresh live source/race audit reached its hard cap. |
| `finite-prefix-cover` | skipped | `20:00:17Z` | `20:00:17Z` | Dependency failure prevented every search shard from starting. |

The raw failing command was:

```text
timeout --signal=TERM --kill-after=3s 60s python scripts/prospective_oeis_a109908_a109909.py live-audit "$RUNNER_TEMP/a1099-input/live-audit.json"
```

It began at `2026-08-14T19:59:13.194Z` and ended at `20:00:13.209Z` with:

```text
Process completed with exit code 124.
```

The next gate-preparation step did not run. The always-run upload then reported:

```text
No files were found with the provided path: /home/runner/work/_temp/a1099-gate/. No artifacts will be uploaded.
```

## Evidence inventory

- GitHub run artifacts: **0** (`total_count: 0`).
- Download audit in a fresh `/tmp` directory: `no valid artifacts found to download`.
- Database-sanity gate bundles: **0**.
- Planned shards: **16**; launched shards: **0**; skipped shards: **16**.
- Shard `execution-status.json` files: **0**.
- Shard `terminal.json` files: **0**.
- Shard `ledger.jsonl` files: **0**.
- Candidate files: **0**.
- Independent artifact-verifier invocations: **0**; there was no artifact eligible to replay.
- Profiles visited: **0**; candidate `n` values evaluated: **0**.

Therefore this run is neither a bounded zero nor evidence about A109908/A109909. It failed closed before the database-sanity gate and before all target evaluation.

## Disposition

Do not retry this frozen version and do not extend the 60-second cap. First create and review a new versioned freeze whose live source/race audit is made deterministically faster while preserving the same fail-closed checks. Only that new exact commit may be dispatched. This invalid run authorizes no release, README result claim, issue, pull request, or novelty statement.
