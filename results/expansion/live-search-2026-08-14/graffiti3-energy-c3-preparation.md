# Graffiti³ energy C3 preparation report

Status: **FROZEN_UNRUN_DEVELOPMENT**

The DOI-qualified source, its current author-repository table, and the complete
local/public duplicate surface were audited before implementation. The audit
found a substantive reading split rather than hiding it: the source table's
center-excluding `d2` convention is already false on `K2` and `K3`, while the
literal closed-ball prose reading passes the 995-graph Atlas gate and becomes
the sole development target.

The committed freeze package is designed to contain:

- source/status and equality-recovery attestation;
- frozen contract and content-hash manifest;
- three deterministic constructors with no pre-commit target evaluation;
- exact candidate escalation through Sturm eigenvalue isolation and radical
  intervals;
- independent freeze, ledger, terminal, and candidate verification, including
  exact candidate-byte binding and full stored-field replay;
- constructor-only and K2-only certificate-tamper tests; and
- a manual exact-commit, read-only GitHub Actions workflow with 54/60 caps.

No eligible order-eight-or-higher graph was target-evaluated while preparing
this freeze. Local validation is limited to parsing/compilation,
constructor-only tests, the source scalar calibration, and the allowed Graph
Atlas controls. The first target evaluation is authorized only after these
files are committed and the workflow is manually dispatched at that exact
commit.

This lane is named `graffiti3-energy-c3`. It is never abbreviated to the
collision-prone `TxGraffiti C3`, which denotes a different product inequality
elsewhere in this repository.
