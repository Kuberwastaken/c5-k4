# Method v1.2 S0

These files are the immutable semantic-source freeze acquired after the public
v1.2 P0 attestation. They are inputs to the attempted registry build, not a
candidate registry, eligible pool, selection, or benchmark result.

Method v1.2 terminated `PROTOCOL_INVALID` before quota feasibility. See the
[failure record](../v1.2-registry-failure/README.md).
