__pycache__/
*.py[cod]
*.olean
/tools/benchmark-drand/node_modules/

# Graphify interpreter/root pointers are machine-local. The graph, report,
# manifest, labels, and static viewer are durable project artifacts.
graphify-out/.graphify_python
graphify-out/.graphify_root
graphify-out/cache/

# regenerable sieve caches (40MB each) — rebuild with scripts/part1/sieves.py
*.npy

# 101MB provenance artifact for a run that selected 0/728 clusters (independent review 2026-08-15)
results/benchmark/v1.4-f0a/registry/provenance-inventory.json
