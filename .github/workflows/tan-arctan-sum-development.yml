name: Tan-arctan-sum exact DEVELOPMENT search

on:
  workflow_dispatch:
    inputs:
      campaign_commit:
        description: Exact 40-hex c5-k4 commit containing the frozen lane
        required: true
        type: string

permissions:
  contents: read

jobs:
  validate-frozen-harness:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Reject a movable execution ref
        shell: bash
        env:
          CAMPAIGN_COMMIT: ${{ inputs.campaign_commit }}
        run: |
          set -euo pipefail
          [[ "$CAMPAIGN_COMMIT" =~ ^[0-9a-f]{40}$ ]]

      - name: Check out the exact frozen campaign commit
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ inputs.campaign_commit }}
          fetch-depth: 0
          persist-credentials: false

      - name: Verify exact clean checkout
        shell: bash
        env:
          CAMPAIGN_COMMIT: ${{ inputs.campaign_commit }}
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$CAMPAIGN_COMMIT"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"

      - name: Set up pinned Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12.9'

      - name: Run constructor-only and frozen-contract tests
        shell: bash
        run: |
          set -euo pipefail
          python -m py_compile scripts/search_tan_arctan_sum_live.py scripts/test_search_tan_arctan_sum_live.py
          PYTHONPATH=scripts python -m unittest -v scripts/test_search_tan_arctan_sum_live.py

  exact-arm:
    needs: validate-frozen-harness
    strategy:
      fail-fast: false
      matrix:
        arm: [CATALOGUE, GENERIC, WALL_NAVIGATION]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    env:
      LC_ALL: C.UTF-8
      PYTHONDONTWRITEBYTECODE: '1'
      PYTHONHASHSEED: '0'
      TZ: UTC
    steps:
      - name: Check out the exact frozen campaign commit
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          ref: ${{ inputs.campaign_commit }}
          fetch-depth: 0
          persist-credentials: false

      - name: Set up pinned Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12.9'

      - name: Run one exact arm under the hard process cap
        id: search
        shell: bash
        env:
          ARM: ${{ matrix.arm }}
        run: |
          set -uo pipefail
          output="$RUNNER_TEMP/tan-arctan-sum/$ARM"
          mkdir -p "$output"
          /usr/bin/time -v -o "$output/resources.txt" \
            timeout --signal=TERM --kill-after=5s 60s \
              python scripts/search_tan_arctan_sum_live.py \
                --arm "$ARM" \
                --ledger "$output/ledger.jsonl" \
                --terminal "$output/terminal.json" \
                --internal-seconds 54 \
              > "$output/stdout.txt" 2> "$output/stderr.txt"
          code=$?
          printf '{"campaign_commit":"%s","arm":"%s","exit_code":%s}\n' \
            '${{ inputs.campaign_commit }}' "$ARM" "$code" > "$output/assignment.json"
          if [[ -f "$output/terminal.json" ]]; then
            python - "$output/terminal.json" <<'PY'
          import json, sys
          value = json.load(open(sys.argv[1], encoding="utf-8"))
          allowed = {
              "DOMAIN_EXHAUSTED", "PROPOSAL_LIMIT", "SEARCH_EXHAUSTED",
              "DEADLINE_PREFIX", "CERTIFICATE_FOUND", "SANITY_GATE_FAILED",
              "WORKER_ERROR",
          }
          if value.get("terminal_reason") not in allowed:
              raise SystemExit("invalid terminal reason")
          PY
          fi
          (
            cd "$output"
            find . -maxdepth 1 -type f ! -name SHA256SUMS -print0 | sort -z | xargs -0 sha256sum > SHA256SUMS
          )
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit "$code"

      - name: Upload complete incremental arm evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: tan-arctan-sum-${{ github.run_id }}-${{ github.run_attempt }}-${{ matrix.arm }}
          path: ${{ runner.temp }}/tan-arctan-sum/
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 90
